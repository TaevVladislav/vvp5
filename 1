#include <iostream>
#include <cmath>
using namespace std;

int main(float z)
{
	int x1, x2, y1, y2;
	cin >> x1;
	cin >> x2;
	cin >> y1;
	cin >> y2;
	z = sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));
	cout << "z = " << z << endl;
}
